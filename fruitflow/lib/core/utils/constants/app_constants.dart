class AppConstants {
  AppConstants._();

  // App Meta
  static const String appName = 'FruitFlow';
  static const int totalLevels = 100;
  static const Duration splashDuration = Duration(seconds: 2);
}